#pragma once
#include <vector>


namespace sort
{
	void selection(std::vector<int>& arr);
	void bubble(std::vector<int>&);
	void sortchars(std::vector<char>&);
	std::vector<int> merge(std::vector<int>& arr);
}

namespace others
{
	int fibonacci(int n);
}

